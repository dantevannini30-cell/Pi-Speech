/**
 * TypeWhisper has been replaced by TextStream (Qwen3-ASR via MLX).
 *
 * See textstream-client.ts for the new streaming STT backend.
 *
 * This file intentionally left as a placeholder to prevent import errors
 * during the migration. It will be removed in a follow-up cleanup pass.
 */
export {};
